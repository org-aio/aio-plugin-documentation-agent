<!-- generated by studio，请勿手工修改。 -->
<template>
  <Dialog v-model="dialogVisible" title="查看委托单样品" width="760">
    <el-descriptions v-loading="loading" :column="2" border>
      <el-descriptions-item label="试样名称">
        {{ detail?.sampleName ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="强度等级规格牌号">
        {{ detail?.strengthGrade ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="工程部位">
        {{ detail?.engineeringLocation ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="成型日期">
        {{ detail?.formingDate ? formatDate(new Date(detail.formingDate)) : '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="炉批号出厂编号">
        {{ detail?.furnaceBatchNumber ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="生产厂家">
        {{ detail?.manufacturer ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="样品数量">
        {{ detail?.sampleQuantity ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="代表批量">
        {{ detail?.representativeBatch ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="样品状态">
        {{ detail?.sampleStatus ?? '-' }}
      </el-descriptions-item>
      <el-descriptions-item label="委托编号">
        {{ detail?.entrustmentNumber ?? '-' }}
      </el-descriptions-item>
    </el-descriptions>
  </Dialog>
</template>

<script setup lang="ts">
import { formatDate } from '@/utils/formatTime'
import * as BoxunCommissionOrderSampleApi from './BoxunCommissionOrderSampleApi'

type BoxunCommissionOrderSampleDetail = Awaited<ReturnType<typeof BoxunCommissionOrderSampleApi.getBoxunCommissionOrderSample>>
type EntityId = string | number

defineOptions({ name: 'BoxunCommissionOrderSampleDetail' })

const dialogVisible = ref(false)
const loading = ref(false)
const detail = ref<BoxunCommissionOrderSampleDetail>()

const open = async (id: EntityId) => {
  detail.value = undefined
  dialogVisible.value = true
  loading.value = true
  try {
    detail.value = await BoxunCommissionOrderSampleApi.getBoxunCommissionOrderSample(id)
  } finally {
    loading.value = false
  }
}
defineExpose({ open })
</script>
