<!-- generated by studio，请勿手工修改。 -->
<template>
  <Dialog
    v-model="dialogVisible"
    :close-on-click-modal="false"
    :title="dialogTitle"
    max-height="60vh"
    scroll
    width="min(760px, calc(100vw - 32px))"
  >
    <el-form ref="formRef" v-loading="formLoading" :model="formData" :rules="formRules" label-position="top">
<el-row class="!mx-0" :gutter="16">
        <el-col :span="12">
          <el-form-item label="项目id" prop="projectId">
            <el-input v-model="formData.projectId" placeholder="请输入项目id" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="是否启用" prop="enableOrNot">
            <el-switch v-model="formData.enableOrNot" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="模板url" prop="templateUrl">
            <el-input v-model="formData.templateUrl" placeholder="请输入模板url" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="模板描述" prop="templateDescription">
            <el-input v-model="formData.templateDescription" :rows="4" placeholder="请输入模板描述" type="textarea" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="模板类型" prop="templateType">
            <el-input-number v-model="formData.templateType" class="!w-100%" controls-position="right" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="字节码包路径" prop="bytecodeRef">
            <el-input v-model="formData.bytecodeRef" placeholder="请输入字节码包路径" />
          </el-form-item>
        </el-col>
</el-row>
    </el-form>

    <template #footer>
      <el-button :disabled="formLoading" type="primary" @click="submitForm">确定</el-button>
      <el-button @click="dialogVisible = false">取消</el-button>
    </template>
  </Dialog>
</template>

<script setup lang="ts">
import * as BoxunTemplateManagementApi from './BoxunTemplateManagementApi'

type BoxunTemplateManagementFormData = Parameters<typeof BoxunTemplateManagementApi.createBoxunTemplateManagement>[0]
type EntityId = string | number


defineOptions({ name: 'BoxunTemplateManagementForm' })

const emit = defineEmits<{ success: [] }>()
const message = useMessage()
const { t } = useI18n()
const dialogVisible = ref(false)
const dialogTitle = ref('')
const formLoading = ref(false)
const formType = ref<'create' | 'update'>('create')
const editingId = ref<EntityId>()
const formRef = ref()
const initialFormData = () => ({
  projectId: '',
  enableOrNot: undefined,
  templateUrl: '',
  templateDescription: '',
  templateType: undefined,
  bytecodeRef: ''
}) as BoxunTemplateManagementFormData
const formData = ref(initialFormData())
const formRules = reactive({

})






const open = async (type: 'create' | 'update', id?: EntityId, defaults?: Partial<BoxunTemplateManagementFormData>) => {

  formType.value = type
  editingId.value = id

  dialogTitle.value = type === 'create' ? '新增模板管理' : '修改模板管理'
  formData.value = { ...initialFormData(), ...defaults }
  formLoading.value = true
  dialogVisible.value = true
  try {


    if (id !== undefined) {
      formData.value = { ...initialFormData(), ...await BoxunTemplateManagementApi.getBoxunTemplateManagement(id) }

    }


  } finally {
    formLoading.value = false
    await nextTick()
    formRef.value?.clearValidate()

  }
}
defineExpose({ open })


const submitForm = async () => {
  if (formLoading.value) return



  const valid = await formRef.value?.validate().catch(() => false)
  if (!valid) {

    return
  }
  const inputFields = formType.value === 'create' ? ['projectId', 'enableOrNot', 'templateUrl', 'templateDescription', 'templateType', 'bytecodeRef'] : ['id', 'projectId', 'enableOrNot', 'templateUrl', 'templateDescription', 'templateType', 'bytecodeRef']
  const input = Object.fromEntries(inputFields.map((key) => [key, formData.value[key]]))

  formLoading.value = true
  try {
    if (formType.value === 'create') {
      await BoxunTemplateManagementApi.createBoxunTemplateManagement(input)
      message.success(t('common.createSuccess'))
    } else {
      await BoxunTemplateManagementApi.updateBoxunTemplateManagement(input)
      message.success(t('common.updateSuccess'))
    }
    dialogVisible.value = false
    emit('success')
  } finally {
    formLoading.value = false
  }
}
</script>
